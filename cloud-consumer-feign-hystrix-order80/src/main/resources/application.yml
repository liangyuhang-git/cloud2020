server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
#ribbon:
  #网络正常，建立连接需要的时间
  #ReadTimeout: 5000
  #建立连接后从服务器获取到可用资源所用的时间
  #Connection: 5000
feign:
  hystrix:
    enabled: true

